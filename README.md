# TS-Foundation-Benchmark
Benchmark comparing ARIMA, LSTM, XGBoost against time-series foundation models (Chronos). Evaluates point accuracy, computational cost, and few-shot learning curves across synthetic datasets.
